<?php

/**
 * Merged over config.php when building with `npm run build`.
 *
 * baseUrl is what every canonical URL, hreflang alternate, sitemap entry and
 * og:image URL is built from. It must match the domain the site is actually
 * served from, or search engines are handed addresses that do not resolve.
 */

return [
    'baseUrl' => 'https://www.monicahq.com',
    'production' => true,
];
